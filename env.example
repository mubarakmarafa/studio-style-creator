#
# Copy these into a `.env.local` for local development.
# On Vercel, add these keys in Project Settings → Environment Variables instead.
#

# Supabase project URL (Supabase Dashboard → Project Settings → API → Project URL)
VITE_SUPABASE_URL=

# Supabase key safe for browser use:
# - Preferred: "publishable" key (starts with sb_publishable_)
# - Also works: legacy "anon" JWT key
VITE_SUPABASE_ANON_KEY=

# Optional override (usually inferred from VITE_SUPABASE_URL):
# - Cloud: https://<project-ref>.supabase.co/functions/v1
# - Local: http://localhost:54321/functions/v1
VITE_SUPABASE_FUNCTIONS_BASE_URL=

# Optional: single-user/no-auth Style Builder "workspace scope" used when creating new projects.
# Reads are NOT scoped by this (all projects are visible to anyone with the URL).
# If set, use the SAME value locally and on Vercel for consistent metadata.
VITE_STYLE_BUILDER_CLIENT_ID=public


